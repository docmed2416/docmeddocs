{
  "filePath": "doc-repo-template/src/types.ts",
  "title": "DocMed Types",
  "content": "This file defines the core type definitions used throughout the DocMed repository.\n\n### Key Exports:\n\n*   **`ManifestEntry`**: Represents an entry in the documentation manifest, containing the file path, title, and a unique document key.\n    ```typescript\n    export type ManifestEntry = { filePath: string; title: string; docKey: string };\n    ```\n*   **`DocVersion`**: Represents a specific version of a document, including its timestamp, title, content, dependencies, and commit ID.\n    ```typescript\n    export type DocVersion = { timestamp: string; title: string; content: string; dependencies: string[]; commitId: string | null };\n    ```\n*   **`DocFile`**: Represents a single documentation file, containing its path, title, content, and optional version history and dependencies.\n    ```typescript\n    export type DocFile = { filePath: string; title: string; content: string; dependencies?: string[]; versions?: DocVersion[]; };\n    ```\n*   **`Commit`**: Represents a Git commit, including its ID, message, pusher, and timestamp.\n    ```typescript\n    export type Commit = { id: string; message: string; pusher: string; timestamp: string };\n    ```\n*   **`DependencyGraph`**: A record mapping document keys to an array of their dependencies.\n    ```typescript\n    export type DependencyGraph = Record<string, string[]>;\n    ```\n*   **`View`**: Discriminated union representing different possible views within the application (home, commits, specific document, folder).\n    ```typescript\n    export type View =\n      | { type: \"home\" }\n      | { type: \"commits\" }\n      | { type: \"doc\"; key: string }\n      | { type: \"folder\"; path: string };\n    ```\n*   **`Tab`**: Represents a tab in the UI, linking an ID, a `View`, and a user-facing label.\n    ```typescript\n    export type Tab = { id: string; view: View; label: string };\n    ```\n\n### Usage Example:\n\nThese types are crucial for ensuring type safety when handling documentation data, Git commits, and UI state within the DocMed application.",
  "dependencies": [],
  "versions": [
    {
      "timestamp": "2026-04-24T17:27:40.513Z",
      "title": "DocMed Types",
      "content": "This file defines the core type definitions used throughout the DocMed repository.\n\n## Key Exports\n\n- `ManifestEntry`: Represents an entry in the documentation manifest, including its file path, display title, and a unique document key.\n- `DocVersion`: Defines the structure for a specific version of a document, including its timestamp, title, content, dependencies, and the Git commit ID.\n- `DocFile`: Represents a single documentation file, containing its path, title, content, optional dependencies, and an array of its historical versions.\n- `Commit`: Represents a Git commit, storing its ID, message, pusher, and timestamp.\n- `DependencyGraph`: A type alias for a record where keys are document keys and values are arrays of document keys that the key document depends on.\n- `View`: A discriminated union type representing different possible views within the DocMed application (e.g., 'home', 'commits', 'deps', 'doc', 'folder').\n- `Tab`: Represents a tab in the UI, containing a unique ID, the associated `View`, and a user-facing label.\n\n## Usage Example\n\n```typescript\nimport type { DocFile, View } from \"./types\";\n\nconst exampleDoc: DocFile = {\n  filePath: \"src/docs/introduction.md\",\n  title: \"Introduction to DocMed\",\n  content: \"# Welcome to DocMed!\\nThis is the introduction.\",\n  dependencies: [\"getting-started\"],\n  versions: [\n    {\n      timestamp: \"2023-10-27T10:00:00Z\",\n      title: \"Initial Draft\",\n      content: \"# Welcome!\\nFirst version.\",\n      dependencies: [],\n      commitId: \"abcdef123\",\n    },\n  ],\n};\n\nconst currentView: View = { type: \"doc\", key: \"introduction\" };\n```",
      "dependencies": [],
      "commitId": null
    },
    {
      "timestamp": "2026-04-24T17:40:29.738Z",
      "title": "DocMed Types",
      "content": "This file defines the core type definitions used throughout the DocMed repository.\n\n### Key Exports:\n\n*   **`ManifestEntry`**: Represents an entry in the documentation manifest, containing the file path, title, and a unique document key.\n    ```typescript\n    export type ManifestEntry = { filePath: string; title: string; docKey: string };\n    ```\n*   **`DocVersion`**: Represents a specific version of a document, including its timestamp, title, content, dependencies, and commit ID.\n    ```typescript\n    export type DocVersion = { timestamp: string; title: string; content: string; dependencies: string[]; commitId: string | null };\n    ```\n*   **`DocFile`**: Represents a single documentation file, containing its path, title, content, and optional version history and dependencies.\n    ```typescript\n    export type DocFile = { filePath: string; title: string; content: string; dependencies?: string[]; versions?: DocVersion[]; };\n    ```\n*   **`Commit`**: Represents a Git commit, including its ID, message, pusher, and timestamp.\n    ```typescript\n    export type Commit = { id: string; message: string; pusher: string; timestamp: string };\n    ```\n*   **`DependencyGraph`**: A record mapping document keys to an array of their dependencies.\n    ```typescript\n    export type DependencyGraph = Record<string, string[]>;\n    ```\n*   **`View`**: Discriminated union representing different possible views within the application (home, commits, specific document, folder).\n    ```typescript\n    export type View =\n      | { type: \"home\" }\n      | { type: \"commits\" }\n      | { type: \"doc\"; key: string }\n      | { type: \"folder\"; path: string };\n    ```\n*   **`Tab`**: Represents a tab in the UI, linking an ID, a `View`, and a user-facing label.\n    ```typescript\n    export type Tab = { id: string; view: View; label: string };\n    ```\n\n### Usage Example:\n\nThese types are crucial for ensuring type safety when handling documentation data, Git commits, and UI state within the DocMed application.",
      "dependencies": [],
      "commitId": "3eb185712c12802c38f8976847fc1fec89f7e8bc"
    }
  ]
}
